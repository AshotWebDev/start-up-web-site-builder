services:
  db:
    image: postgres:18-alpine
    environment:
      POSTGRES_DB: sitebuilder_db
      POSTGRES_USER: vardan1444
      POSTGRES_PASSWORD: Zibilanoc3!
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    restart: unless-stopped

volumes:
  postgres_data: